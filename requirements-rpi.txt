# manual_drive — Raspberry Pi hardware dependencies
# Install on Raspberry Pi only: pip install -r requirements-rpi.txt
# NOT required for PC mock mode or development.

RPi.GPIO>=0.7.0
picamera2>=0.3.0
